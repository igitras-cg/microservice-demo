spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8088
  datasource:
    url: jdbc:mysql://localhost:3306/ms_oauth?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

logging:
  level:
    org.springframework.security: debug

